import 'package:flutter/material.dart';

class FormReservaPage extends StatelessWidget {
  const FormReservaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Formulario de Reserva")),
      body: const Center(
        child: Text("Formulario en construcción"),
      ),
    );
  }
}
